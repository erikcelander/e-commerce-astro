---
// import type { Product } from "@/lib/types";

// export interface Props {
//   product: Product;
// }

const { product } = Astro.props;
---

<a
  class="flex outline-none focus:outline-none"
  href={`/products/${product.id}`}
>
  <div
    class="bg-card text-card-foreground w-64 transform rounded-lg border shadow-sm transition duration-300 ease-in-out hover:-translate-y-1 hover:shadow-lg"
    data-v0-t="card"
  >
    <img
      src={product.image}
      alt={product.name}
      class="h-64 w-full rounded-t-lg object-cover"
      width="200"
      height="200"
    />
    <div class="p-6">
      <h2 class="text-lg font-semibold">{product.name}</h2>
      <p class="text-gray-500">{product.description}</p>
      <div class="mt-4 flex items-center justify-between">
        <span class="text-xl font-bold">{` ${product.price} ${product.currency}`}</span>
        <div
          class={`h-4 w-4 rounded-full ${product.stock > 0 ? "bg-green-500" : "bg-red-500"}`}
        ></div>
      </div>
    </div>
  </div>
</a>
